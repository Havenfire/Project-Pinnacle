type Pin @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  description: String
  coordinates: [Float!]
  reputation: Int!
  StoreImage: StoreImage @hasOne
}

type StoreImage @model @auth(rules: [{allow: public}]) {
  id: ID!
  file_name: String!
}
 